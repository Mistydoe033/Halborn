# Running Tests

To run all tests for the vulnerability analysis:

```bash
cargo test --workspace --lib
```

This will execute all test cases including the vulnerability demonstration tests across all three directories.

